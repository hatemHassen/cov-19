{% extends 'front/base.html.twig' %}
{% trans_default_domain 'patient' %}
{% block content %}
<div class="patient create pt-120">
    <div class="row-cols-auto">
        <div class="offset-3 col-md-6 col-sm-12">
            <div class="contact-form-wrapper">
                <h4 class="mb-40 wow fadeInUp" data-wow-delay=".3s" style="visibility: visible; animation-delay: 0.3s; animation-name: fadeInUp;">{{'patient.createPatientAction.newPatient'|trans}}</h4>
                {% include 'front/flashMessage.html.twig' %}
                {{ form(form) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
