{% extends 'form_div_layout.html.twig' %}


{%- block choice_widget -%}
    {% if expanded %}
        {{- block('choice_widget_expanded') -}}
    {% else %}
        {{- block('choice_widget_collapsed') -}}
    {% endif %}
{%- endblock choice_widget -%}

{%- block choice_widget_expanded -%}
    {%- set attr = attr|merge({class: (attr.class|default('') ~ ' p-v-xs')|trim }) -%}
    <div {{ block('widget_container_attributes') }}>
        {%- for child in form %}
            {% if child.vars.block_prefixes.2 == 'radio' %}
                <p>
                    {{- form_widget(child) -}}
                    <label>
                        <span> {{- form_label(child, null, {translation_domain: choice_translation_domain,required:false}) -}}</span>
                    </label>
                </p>

            {% else %}
                {{- form_widget(child,{attr: {class: "filled-in"}}) -}}
            {% endif %}

        {% endfor -%}
    </div>
{%- endblock choice_widget_expanded -%}


{%- block choice_widget_collapsed -%}
    {%- if required and placeholder is none and not placeholder_in_choices and not multiple and (attr.size is not defined or attr.size <= 1) -%}
        {% set required = false %}
    {%- endif -%}
    <select {{ block('widget_attributes') }}{% if multiple %} multiple="multiple"{% endif %}>
        {%- if placeholder is not none -%}
            <option value=""{% if required and value is empty %} selected="selected"{% endif %}>{{ placeholder != '' ? (translation_domain is same as(false) ? placeholder : placeholder|trans({}, translation_domain)) }}</option>
        {%- endif -%}
        {%- if preferred_choices|length > 0 -%}
            {% set options = preferred_choices %}
            {{- block('choice_widget_options') -}}
            {%- if choices|length > 0 and separator is not none -%}
                <option disabled="disabled">{{ separator }}</option>
            {%- endif -%}
        {%- endif -%}
        {%- set options = choices -%}
        {{- block('choice_widget_options') -}}
    </select>
{%- endblock choice_widget_collapsed -%}

{%- block checkbox_widget -%}
    <div class="switch">
        <input type="checkbox"{{ block('widget_attributes') }}{% if value is defined %} value="{{ value }}"{% endif %} {% if checked %} checked="checked" {% endif %} />
        <label>
            {{ form.vars.label|trans([],translation_domain) }}
        </label>
    </div>
{%- endblock checkbox_widget -%}
